# ModernFM Environment Configuration

# --- 端口配置 ---
# Web UI 访问端口 (浏览器访问端口)
WEB_UI_PORT=80
# 后端 API 端口
API_PORT=38866

# --- 存储配置 ---
# 宿主机上需要被管理的文件路径 (例如 Unraid 的 /mnt/user)
HOST_DATA_PATH=/mnt/user
# 容器内部挂载根目录 (通常保持默认 /data)
FILE_ROOT_DIR=/data

# --- 数据库配置 ---
POSTGRES_USER=modernfm_user
POSTGRES_PASSWORD=secure_pass_123
POSTGRES_DB=modernfm
